@model IEnumerable<LTW_Day09Lab_CodeFirst.Models.TvcLoaiSanPham>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.tvcMaLoai)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.tvcTenLoai)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.tvcTrangThai)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.tvcMaLoai)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.tvcTenLoai)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.tvcTrangThai)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.tvcId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.tvcId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.tvcId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
